// SaveRecipesToDatabase.ts

// Function to print a recipe with a given reason and content
export function SaveRecipesToDatabase(reason: string, content: string): void {
  console.log("PRINTG THE RECIPE FROM SAVERECIPESANDDESSERTSTODATABASE");
  console.log(`Reason: ${reason}`);
  console.log(`Recipe Content:\n${content}`);
}
